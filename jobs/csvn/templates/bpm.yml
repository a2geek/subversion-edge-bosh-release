# /var/vcap/jobs/archiva/config/bpm.yml
processes:
- name: csvn
  executable: /var/vcap/jobs/csvn/bin/ctl
  workdir: /var/vcap/jobs/csvn/packages/subversion-edge

  #ephemeral_disk: true
  #persistent_disk: true
  additional_volumes:
  - path: /var/vcap/jobs/csvn/packages/subversion-edge/data/repositories
    ???

  capabilities:
  - NET_BIND_SERVICE
